/**
 * Litige — accès aux données
 *
 * Les seules requêtes SQL du domaine. **Interne au module** : aucun autre
 * module n’a le droit d’importer ce fichier, la règle est appliquée par
 * ESLint.
 */

export const depot = {} as const;
