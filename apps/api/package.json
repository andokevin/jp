{
  "name": "@jp/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify — service unique modulaire.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  }
}
